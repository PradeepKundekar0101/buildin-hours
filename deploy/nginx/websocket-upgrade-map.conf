# Include this from the `http {}` block in /etc/nginx/nginx.conf (one time,
# shared by all server blocks) - it's what makes $connection_upgrade in
# api.panchayatai.com.conf resolve.
map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
